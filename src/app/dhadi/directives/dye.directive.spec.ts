import { DyeDirective } from './dye.directive';

describe('DyeDirective', () => {
  it('should create an instance', () => {
    const directive = new DyeDirective();
    expect(directive).toBeTruthy();
  });
});

import { DhadiDirective } from './dhadi.directive';

describe('DhadiDirective', () => {
  it('should create an instance', () => {
    const directive = new DhadiDirective();
    expect(directive).toBeTruthy();
  });
});
